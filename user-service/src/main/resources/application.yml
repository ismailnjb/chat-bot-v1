server:
  port: 9081

spring:
  datasource:
    url: jdbc:postgresql://172.18.0.2:5432/userdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: user-service
  docker:
    compose:
      file: ../docker-compose.yml
      enabled: false
